{
  "releaseType": "patch",
  "description": "Add enhanced logging for Global Status debugging",
  "changes": [
    "Added detailed logging for server configuration discovery",
    "Added logging for each server connection attempt",
    "Added logging for container fetching from each server",
    "Added error message broadcasting to clients when status fetch fails"
  ],
  "features": [
    "Better error reporting when Global Status fails to load",
    "More detailed logs for debugging multi-server issues"
  ],
  "fixes": [
    "Global Status showing 'Loading...' indefinitely without error",
    "No feedback when server configuration is missing",
    "Silent failures when server connections fail"
  ],
  "breaking": [],
  "notes": "Enhanced logging to diagnose Global Status loading issues"
}

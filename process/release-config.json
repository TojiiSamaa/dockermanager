{
  "releaseType": "patch",
  "description": "Fix Global Status not loading and improve settings persistence",
  "changes": [
    "Fixed critical bug where server settings were not persisted to Stream Deck storage",
    "Added extensive logging to settings manager for debugging",
    "Added console logging to Global Status page for troubleshooting",
    "Improved error handling when sending WebSocket messages",
    "Added immediate empty status message when no servers configured"
  ],
  "features": [
    "Global Status now shows 'No servers configured' message immediately",
    "Better client-side diagnostics with browser console logging"
  ],
  "fixes": [
    "Global Status page stuck on 'Loading servers...' indefinitely",
    "Server configurations not being saved properly between plugin restarts",
    "Silent failures in WebSocket message broadcasting"
  ],
  "breaking": [],
  "notes": "This release fixes the core issue where Global Status was not loading. The problem was that server settings were cached in memory but not persisted to Stream Deck's storage, causing them to be unavailable when Global Status tried to fetch them."
}

{
  "releaseType": "patch",
  "description": "Fix critical multi-server architecture interference",
  "changes": [
    "Refactored getContainerHealth to accept server config parameter",
    "Added server-specific versions of all container methods",
    "Removed all dockerService.configure() calls that changed global state",
    "Updated docker-toggle and docker-logs to use server-specific methods"
  ],
  "features": [
    "Each button now maintains independent server connection",
    "Multiple buttons can monitor different servers simultaneously without interference"
  ],
  "fixes": [
    "Container health showing incorrect status when multiple servers configured",
    "Buttons interfering with each other when monitoring same container on different servers",
    "Connection state changing unexpectedly when multiple buttons update"
  ],
  "breaking": [],
  "notes": "Critical fix for multi-server functionality - each button now stays on its configured server independently"
}
